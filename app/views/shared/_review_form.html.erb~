<% @review = Review.new %>
<%= form_for(@review) do |f| %>
  <% div class="field" %>
  <%= f.hidden_field :book_id, :value => @book.id  %>
  <%= f.text_area :text, placeholder: "Write a review for this book..." %>
  <%= f.submit "Post Review", class: "btn btn-large btn-primary" %>
<% end %>
