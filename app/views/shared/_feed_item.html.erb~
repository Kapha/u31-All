<li id="<%= feed_item.id %>">
  <%= link_to gravatar_for(feed_item.user), feed_item.user %>
  <span class="user">
    <%= link_to feed_item.user.username, feed_item.user %>
  </span>

  <% if defined? feed_item.content %>
  </br>
  <span class="content"><%= feed_item.content %></span>
  <% end %>
  <% if defined? feed_item.text %>
  reviewed
  <%= link_to feed_item.book.title, feed_item.book %> 
  by 
  <%= link_to feed_item.book.author.afull_name, feed_item.book.author %>
  as
  <% if feed_item.rating = 1 %>
    "Awesome"
  <% end %>
  <% if feed_item.rating = 2 %>
    "Good"
  <% end %>
  <% if feed_item.rating = 3 %>
    "OK"
  <% end %>
  <% if feed_item.rating = 4 %>
    "No Good"
  <% end %>
  <% if feed_item.rating = 5 %>
    "Absolutely Horrible"
  <% end %>
  <br />
  <span class="content">
  <%= feed_item.text %>
  </span>
  <% end %>
  </br>
  <span class="timestamp">
    Posted <%= time_ago_in_words(feed_item.created_at) %> ago.
  </span>
</li>
